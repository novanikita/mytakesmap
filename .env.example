# PostgreSQL — Neon (use the pooled connection string)
# https://console.neon.tech → project → Connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Auth.js — openssl rand -base64 32
AUTH_SECRET=

# Public site URL (needed for Auth on Vercel)
AUTH_URL=https://www.mytakesmap.space

# Vercel Blob — Storage → Blob → token
# BLOB_READ_WRITE_TOKEN=
