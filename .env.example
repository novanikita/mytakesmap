# Copy to .env.local for local admin editing

# Your machine (editing enabled):
NEXT_PUBLIC_IS_ADMIN=true
NEXT_PUBLIC_ADMIN_SECRET=choose-a-secret
ADMIN_SECRET=choose-a-secret

# Public deploy (view only):
# Do not set NEXT_PUBLIC_IS_ADMIN
# Keep ADMIN_SECRET on the server to lock write APIs
